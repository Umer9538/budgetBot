# ===========================================
# BudgetBot Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# ===========================================

# -------------------------------------------
# PLAID API (Bank Account Linking)
# -------------------------------------------
# Get your credentials at: https://dashboard.plaid.com/signup
#
# Environments:
#   - sandbox: Free testing with test credentials
#   - development: Test with real banks (limited requests)
#   - production: Live production use
#
PLAID_CLIENT_ID=your_plaid_client_id_here
PLAID_SECRET=your_plaid_secret_here
PLAID_ENV=sandbox

# Plaid Products to request (comma-separated)
# Options: transactions, auth, identity, assets, investments, liabilities
PLAID_PRODUCTS=transactions

# Plaid Country Codes (comma-separated)
# Options: US, CA, GB, IE, FR, ES, NL, DE
PLAID_COUNTRY_CODES=US

# -------------------------------------------
# OPENAI API (AI Categorization)
# -------------------------------------------
# Get your API key at: https://platform.openai.com/api-keys
#
# Pricing: ~$0.002 per 1K tokens (very cheap for categorization)
# Model: gpt-3.5-turbo (fast and cost-effective)
#
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# -------------------------------------------
# FIREBASE (Optional - usually auto-configured)
# -------------------------------------------
# These are typically set in google-services.json (Android)
# and GoogleService-Info.plist (iOS)
# Only set here if using web or need override
#
# FIREBASE_API_KEY=
# FIREBASE_PROJECT_ID=
# FIREBASE_MESSAGING_SENDER_ID=
# FIREBASE_APP_ID=

# -------------------------------------------
# APP CONFIGURATION
# -------------------------------------------
# Environment: development, staging, production
APP_ENV=development

# Enable debug logging
DEBUG_MODE=true

# Background sync interval in hours (default: 4)
SYNC_INTERVAL_HOURS=4

# Historical transaction import (months back)
HISTORICAL_IMPORT_MONTHS=24
